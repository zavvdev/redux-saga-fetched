export const DEFAULT_STALE_TIME_MS = Infinity;
export const DEFAULT_CACHE_TIME_MS = 5 * 60 * 1000;

export const MESSAGES = {
  error: {
    domainIsString: "InitOptions.domain must be a string",
  },
};
